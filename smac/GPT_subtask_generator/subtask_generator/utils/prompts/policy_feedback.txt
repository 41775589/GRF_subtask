We trained a RL policy using the provided reward function code and tracked the values of the individual components in the reward function as well as global policy metrics after every {epoch_freq} epochs, mean values encountered:
